# Copy to .env and fill in (never commit real keys)
# Delta Exchange API (base URL + keys go here)
DELTA_BASE_URL=https://api.delta.exchange
DELTA_API_KEY=
DELTA_API_SECRET=

# Only needed for REPLAY mode (validating against history on your local DB)
DATABASE_URL=postgresql://admin:admin@localhost:5432/delta
